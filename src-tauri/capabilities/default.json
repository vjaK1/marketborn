{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC (events + app commands)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
